@{
    ViewBag.Title = "Lembretes";
}

<div class="container mt-4">

    <!-- Botão Adicionar Lembrete -->
    <div style="d-flex justify-content-between align-items-center mb-4; margin-bottom: 30px;">
        <h2 class="fw-bold">
            <i class="bi bi-bell-fill"></i> Lembretes
        </h2>
        <a>
            <button class="btn btn-primary btn-lg" data-bs-toggle="modal" data-bs-target="#novoLembreteModal"
                style="background-color: #0ea5e9; border: none; padding: 12px 30px; border-radius: 8px;">
                <i class="bi bi-plus-circle"></i> Novo Lembrete
            </button>
        </a>
    </div>

    @*<!-- TÍTULO -->
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2 class="fw-bold">
                <i class="bi bi-bell-fill"></i> Lembretes
            </h2>

            <a class="btn btn-lg btn-primary" href="/Lembretes/Create">
                <i class="bi bi-plus-circle"></i> Criar Novo Lembrete
            </a>
        </div>@**@

    <!-- MENSAGEM DE INTRODUÇÃO -->
    <div class="alert alert-primary p-4 fs-5 shadow-sm">
        <strong>Bem-vindo(a)!</strong><br>
        Aqui você pode ver seus lembretes importantes para o dia a dia.
        Este sistema foi feito para ajudar pessoas como você a não esquecer tarefas importantes.
    </div>

    <!-- CARDS DE DESTAQUE -->
    <div class="row mb-4">
        @if (ViewBag.ListaLembretes != null && ViewBag.ListaLembretes.Count > 0)
        {
            @foreach (Lembrete lembrete in ViewBag.ListaLembretes)
            {
                <div class="col-md-4">
                    <div class="card shadow-sm border-1">
                        <div class="card-header d-flex justify-content-between">
                            <h5 class="card-title">@lembrete.Titulo</h5>

                            <div class="dropdown">
                                <button class="btn btn-link text-dark p-0" data-bs-toggle="dropdown">
                                    <i class="bi bi-three-dots-vertical"></i>
                                </button>

                                <ul class="dropdown-menu dropdown-menu-end">
                                    <li><a class="dropdown-item"  onclick="eliminarLembrete(@lembrete.Id)"  href="#"><i class="bi bi-trash"></i> Excluir</a></li>
                                </ul>
                            </div>
                        </div>
                        <div class="card-body">
                            <p class="card-text">@lembrete.Descricao</p>

                            <div class="">
                                @if (lembrete.Prioridade == 3)
                                {
                                    <span class="w-25 badge bg-danger">Alta</span>
                                }
                                else if (lembrete.Prioridade == 2)
                                {
                                    <span class="w-25 badge bg-warning">Média</span>
                                }
                                else
                                {
                                    <span class="w-25 badge bg-success">Baixa</span>
                                }


                                <span
                                    class="w-25 badge text-black text-start fs-6">@(lembrete.DataLembrete?.ToString("dd/MM/yyyy"))</span>
                            </div>
                        </div>

                        <div class="card-footer">
                            @if (lembrete.ContatoLembretes?.Count > 0)
                            {
                                <span class="fw-semibold fs-6">Contatos</span>
                                <div class="gap-2 d-flex mb-3">

                                    @foreach (ContatoLembrete contatos in lembrete.ContatoLembretes)
                                    {
                                        <span class="badge text-secondary fs-6 fw-medium">@contatos.Contatos.Nome</span>
                                    }
                                </div>
                            }

                            @if (lembrete.LugaresLembretes?.Count > 0)
                            {
                                <span class="fw-semibold fs-6">Lugares</span>
                                <div class="gap-2 d-flex">
                                    @foreach (LugaresLembrete lugares in lembrete.LugaresLembretes)
                                    {
                                        <span class="badge text-secondary fs-6 fw-medium">@lugares.Lugares.Nome</span>
                                    }
                                </div>
                            }
                        </div>
                    </div>
                </div>
            }
        }
    </div>

    @* 
    <!-- CARDS DE DESTAQUE -->
    <div class="row mb-4">
        <div class="col-md-4">
            <div class="card shadow-sm border-1">
                <div class="card-body">
                    <h5 class="card-title"><i class="bi bi-calendar-event"></i> Próxima Tarefa</h5>
                    <p class="card-text">Tomar remédio às 10h.</p>
                    <span class="badge bg-warning text-dark">Hoje</span>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card shadow-sm border-1">
                <div class="card-body">
                    <h5 class="card-title"><i class="bi bi-check2-circle"></i> Tarefas Concluídas</h5>
                    <p class="card-text">2 lembretes concluídos nesta semana.</p>
                    <span class="badge bg-success">Bom trabalho!</span>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card shadow-sm border-1">
                <div class="card-body">
                    <h5 class="card-title"><i class="bi bi-exclamation-circle"></i> Importante</h5>
                    <p class="card-text">Consulta médica amanhã às 14h.</p>
                    <span class="badge bg-danger">Atenção!</span>
                </div>
            </div>
        </div>
    </div>
    <!-- CARD DE DESTAQUE -->
    <div class="card mb-3 shadow-sm border-1">
        <div class="card-body">
            <h5 class="card-title">
                <i class="bi bi-bell"></i> Tomar remédio
            </h5>
            <p class="card-text">Medicamento às 10h</p>
            <span class="badge bg-warning text-dark">Alta prioridade</span>
        </div>
    </div> *@

    <!-- Modal Novo Lugar -->
    <div class="modal fade" id="novoLembreteModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title bi bi-plus-circle">Criar Novo Lembrete</h5>
                    <a class="btn btn-lg btn-primary" href="/Lembretes/Create">

                    </a>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>

                <form method="post" class="modal-body" asp-action="AdicionarLembrete" asp-controller="Lembretes">
                    <div class="modal-body">
                        <div class="mb-3">
                            <label for="nome" class="form-label">Título</label>
                            <input type="text" class="form-control" id="Título" name="Titulo" required>
                        </div>

                        <div class="mb-3">
                            <label for="nome" class="form-label">Descrição</label>
                            <input type="text" class="form-control" id="Descrição" name="Descricao" required>
                        </div>

                        <div class="mb-3">
                            <label for="nome" class="form-label">Data</label>
                            <input type="date" class="form-control" id="Data" name="DataLembrete" required
                                placeholder="dd/mm/aaaa">
                        </div>

                        <div class="mb-3">
                            <label for="Concluído" class="form-label">Prioridade</label>
                            <select class="form-select" id="Concluído" name="Prioridade" required>
                                <option value="3">Alta</option>
                                <option value="2">Média</option>
                                <option value="1">Baixa</option>
                            </select>

                            <div class="mb-3">
                                <label for="Concluído" class="form-label">Concluído</label>
                                <select class="form-select" id="Concluído" name="Status" required>
                                    <option value="F">Sim</option>
                                    <option value="P">Não</option>
                                </select>
                            </div>

                            <div class="mb-3">
                                <label for="contatos-lista" class="form-label">Selecione um ou mais contatos</label>
                                <select id="contatos-lista" name="contatos" class="form-select" multiple
                                    style="width: 100%;">
                                    @if (ViewBag.ListaContatos != null && ViewBag.ListaContatos.Count > 0)
                                    {
                                        @foreach (Contato contato in ViewBag.ListaContatos)
                                        {
                                            <option value="@contato.Id">@contato.Nome</option>
                                        }
                                    }
                                </select>
                            </div>

                            <div class="mb-3">
                                <label for="lugares-lista" class="form-label">Selecione um ou mais lugares</label>
                                <select id="lugares-lista" name="lugares" class="form-select" multiple
                                    style="width: 100%;">
                                    @if (ViewBag.ListaLugares != null && ViewBag.ListaLugares.Count > 0)
                                    {
                                        @foreach (Lugare lugares in ViewBag.ListaLugares)
                                        {
                                            <option value="@lugares.Id">@lugares.Nome</option>
                                        }
                                    }
                                </select>
                            </div>

                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                            <button type="submit" class="btn btn-primary">Criar Lembrete</button>
                        </div>
                </form>
            </div>
        </div>
    </div>

    <!-- ÁREA DE DICAS -->

    <footer class="text-center mt-5 mb-3">
        <div class="container">
            <div class="alert alert-light border rounded p-3 mx-auto" style="max-width: 9900px;">
                <strong>💡 Dicas para organizar seus lembretes</strong>
                <ul class="mb-0 mt-2 text-start">
                    <li>Use lembretes curtos e fáceis de entender.</li>
                    <li>Marque como concluído quando terminar uma tarefa.</li>
                    <li>Evite acumular muitos lembretes antigos.</li>
                    <li>Use cores e prioridades para lembrar o que é mais importante.</li>
                </ul>
            </div>
            <small class="text-muted">© 2025 - RecentMemory - <a href="#">Privacy</a></small>
        </div>
    </footer>


</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('#lugares-lista').select2({
                dropdownParent: $('#novoLembreteModal')
            });

            $('#contatos-lista').select2({
                dropdownParent: $('#novoLembreteModal')
            });
        });


        function eliminarLembrete(lembreteId) {
            if (confirm("Você tem certeza que quer remover o lembrete?")) {
                fetch("/Lembretes/Remover/" + lembreteId, {
                    method: "DELETE"
                });
            }
        }


        function editarLembrete(lembreteId) {
            if (confirm("Você tem certeza que quer editar o lembrete?")) {

                fetch("/Lembretes/Editar/" + lembreteId, {
                    method: "PUT"
                });
            }
        }

    </script>
}